# Core dependencies
opencv-python>=4.7
face_recognition>=1.3.0
numpy>=1.22
pandas>=1.5
openpyxl>=3.1
cryptography>=42.0.0
PyQt5>=5.15.0
matplotlib>=3.7

# The following are part of the Python standard library and typically do not need installation via pip:
# tkinter  # stdlib (ensure Python is installed with Tk support)
# sqlite3  # stdlib
